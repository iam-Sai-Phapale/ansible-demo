---
- name: this is my shell playbook
  hosts: all
  become: true
  tasks:
    - shell: echo "My Name is Sai Phapale" >> /mnt/analysis.txt
    - name: This our first shell task
      debug:
        msg: "{{ansible_distribution}}"
...